{
  "name": "trinity-capture",
  "version": "0.3.6",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/atheonxyz/trinity-capture.git"
  },
  "scripts": {
    "typecheck": "tsc -b",
    "build:plugin": "tsc -p tsconfig.plugin.json",
    "build:plugin-cursor": "tsc -p tsconfig.plugin-cursor.json",
    "build:codex": "tsc -p tsconfig.codex-plugin.json",
    "test": "node --test dist-test/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
